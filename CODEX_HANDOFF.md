# Codex handoff — final Guild × Cinemaverse recording

## Current state

Guild's locked product scope is implemented, tested, merged, and deployed. The separate
Cinemaverse vertical slice, hosted six-screen preview, deterministic research fixture, and verified
V1/V2 design checkpoints exist. The current implementation and production evidence are recorded in
`IMPLEMENTATION_STATUS.md`.

The rejected demo capture is not a deliverable and must never be reused. The remaining task is to
create a new Guild workspace titled `Cinemaverse`, rehearse the approved route twice, record a new
silent master, and assemble the final video after presenter media is available.

## Canonical handoff

Use `DEMO_IMPLEMENTATION_PROMPT.md` as the complete new-session execution contract. Read and follow
the referenced files in the order it specifies.

The recording must preserve these decisions:

- visible Chrome uses the `avichaldwivedi2005@gmail.com` session, never Harshita's profile;
- a hidden in-app browser may supply native WebMCP while Chrome displays realtime state;
- the new workspace is distinct from `Guild Judge Workspace` and prior Cinemaverse workspaces;
- Guild remains visible after the landing page, including while Cinemaverse is interacted with;
- six large agent-owned canvas regions are the primary visual;
- five real Codex responsibilities and one bounded Claude Sonnet responsibility run;
- the initial Runner is preflighted at six stable slots so all six Jobs visibly overlap;
- Claude uses Sonnet, never Fable;
- artifacts contain detailed, expandable Cinemaverse-specific content;
- the Agent dock is only a brief status view;
- one Codex artifact and two hosted design regions are annotated, with both design notes grouped
  into one Claude request before one Send;
- the six visible identities are Runner Jobs; mirrored external workstreams are forbidden;
- the revised architecture and V2 design are compared and human-approved in the new workspace;
- existing verified V1/V2 checkpoints may be reused honestly to save credits;
- the private Cinemaverse source repository is available at verified baseline `a0d2339`;
- pull-request evidence, when present, is read inside Guild Evidence Focus with branch, commit,
  changed files, checks, and provenance; do not leave Guild to show GitHub during the take;
- the reserved V1/V2 aliases remain blocked by Vercel Authentication until direct signed-out 200
  and iframe/capture checks pass; never use a bypass token in Guild metadata;
- an absent optional Preview Bridge uses revision/route/viewport/coordinate anchors and is not
  presented as a failure or stable DOM-element tracking;
- Chrome automation is disconnected before recording so its debugging banner is absent;
- no agent chat, chain-of-thought, fake progress, fake generation, secret, or rejected footage is
  shown; and
- the video ends on Guild's complete canvas.

## Start command for a new chat

Paste the fenced prompt from `HANDOFF_PROMPT.md`, or attach this repository and say:

> Execute `DEMO_IMPLEMENTATION_PROMPT.md` completely. Prepare the new Cinemaverse workspace,
> rehearse twice, and record the replacement Guild demo. Do not reuse the rejected capture.

## Completion

The next session is done only after the master and silent edit exist and have been visually and
technically verified. The final face-camera composite may remain pending only when the user has not
yet supplied presenter footage or narration.
